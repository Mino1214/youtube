# main.py 사용 가이드

## 🚀 간단한 실행

가장 간단한 방법으로 비디오를 생성할 수 있습니다:

```bash
python main.py
```

이 명령어 하나로 전체 파이프라인이 실행됩니다!

## 📝 입력 방법

### 방법 1: input_text.txt 파일 사용 (권장)

1. `input_text.txt` 파일을 열기
2. 영어 텍스트 입력
3. 저장
4. `python main.py` 실행

### 방법 2: main.py 파일 직접 수정

1. `main.py` 파일 열기
2. `english_text` 변수 찾기
3. 원하는 영어 텍스트로 수정
4. 저장
5. `python main.py` 실행

## 📋 실행 흐름

```
영어 텍스트 입력
    ↓
[1] 영어 TTS 생성
    ↓
[2] 영어 비디오 생성 (텍스트 슬라이드쇼)
    ↓
[3] STT 처리 (영어 오디오 → 텍스트)
    ↓
[4] 영어 → 한국어 번역 (로컬 LLM)
    ↓
[5] 한국어 TTS 생성
    ↓
[6] 한국어 자막 생성
    ↓
[7] 영어 비디오 음소거
    ↓
[8] 최종 비디오 렌더링 (한국어 나레이션 + 자막)
    ↓
output_video.mp4 생성!
```

## ⚙️ 설정

`config.yaml` 파일에서 다음 설정을 변경할 수 있습니다:

```yaml
video_generation:
  width: 1920        # 비디오 너비
  height: 1080      # 비디오 높이
  fps: 30           # 프레임레이트
  background_color: "#000000"  # 배경색
```

## 📁 출력

생성된 비디오는 `output_video.mp4`로 저장됩니다.

## 💡 팁

- 첫 실행은 모델 다운로드로 인해 시간이 오래 걸립니다
- 짧은 텍스트로 먼저 테스트해보세요
- `input_text.txt` 파일을 사용하면 편리합니다

## 🔧 문제 해결

### 텍스트가 없습니다 오류
- `input_text.txt` 파일을 생성하거나
- `main.py`의 `english_text` 변수를 수정하세요

### FFmpeg 오류
- FFmpeg가 설치되어 있는지 확인
- PATH 환경 변수에 추가되어 있는지 확인

### 모델 다운로드 실패
- 인터넷 연결 확인
- HuggingFace 토큰 설정 (필요시)

